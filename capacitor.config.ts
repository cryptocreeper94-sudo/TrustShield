import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darkwave.trustshield',
  appName: 'TrustShield',
  webDir: 'dist/client'
};

export default config;
